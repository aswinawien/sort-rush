# Flutter + Flame Implementation

Act as the Implementation Agent. Read all relevant docs before changing code. Implement only the approved slice.

Use Flutter for navigation, menus, overlays, settings, and result screens. Use Flame for the active loop, package components, movement, timing, effects, and gameplay input. Keep gameplay calculations pure where possible, use seedable randomness, store difficulty in configuration, and keep navigation out of Flame components.

Before editing: state files, risks, tests, and non-goals. After editing: run `flutter analyze`, `flutter test`, and the relevant scripted scenarios. Report every failure. Do not invent Flame APIs; verify version-sensitive APIs from official documentation.
